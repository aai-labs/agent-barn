// The active org id that the default org-list mock (interceptGetOrganizationsRequest)
// returns, so org-scoped URLs in tests resolve to an org the (platform_admin) fixture can see.
export const TEST_ORG_ID = "22222222-2222-4222-8222-222222222222";
